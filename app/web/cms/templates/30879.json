{"content":{"type":"API","structure":"","server_on_load":"async ({\n  template,\n  params,\n  render,\n  db,\n  req,\n  reply,\n  user,\n  log,\n  ext,\n  isDev,\n}: Server) => {\n  const { email, task } = req.body;\n  var verifToken;\n  if (task === \"web\") {\n    verifToken = ext.generateLongToken();\n  } else {\n    verifToken = ext.generateToken();\n  }\n\n  const users = await db.users.findUnique({\n    where: {\n      email,\n    },\n  });\n\n  if (!users) {\n    return reply.status(404).send({\n      status: \"ERROR\",\n      message:\n        \"Your account hasn’t registered yet. Make sure the email you entered is correct.\",\n    });\n  }\n\n  if (task === \"web\") {\n    await ext.sendVerifEmail(email, verifToken);\n    await db.users.update({\n      where: {\n        email,\n      },\n      data: {\n        token: verifToken,\n      },\n    });\n  } else {\n    await ext.sendToken(email, verifToken);\n    var dataUser = await db.users.update({\n      where: {\n        email,\n      },\n      data: {\n        token: verifToken,\n      },\n    });\n\n    user.user = { role: \"Guest\", dataUser };\n  }\n\n  reply.status(200).send({\n    status: \"SUCCESS\",\n    message: \"Berhasil login\",\n    data: verifToken,\n  })\n}","figma":{}},"title":"send-email","type":"cms-template","lang":"","status":"SYSTEM","parent_id":"66283","slug":"/api/send-email","site":"*","id":"30879"}